[package]
name = "sider"
version = "0.1.0"
edition = "2024"
description = "In-memory database server with an incremental Redis-compatible core"
repository = "https://github.com/djairofilho/sider"
license = "MIT"
publish = false

[dependencies]
bytes = "1"
thiserror = "2"

[dev-dependencies]
proptest = "1"
serde_json = "1"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
